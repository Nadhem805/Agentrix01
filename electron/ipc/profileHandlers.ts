import type { IpcMain } from 'electron'

// Handles: profile:check, profile:create, workspace:list, workspace:create
export function registerProfileHandlers(_ipcMain: IpcMain) {
  // TODO: implement in Phase 2
  // ipcMain.handle('profile:check', async () => { ... })
  // ipcMain.handle('profile:create', async (_, data) => { ... })
  // ipcMain.handle('workspace:list', async () => { ... })
  // ipcMain.handle('workspace:create', async (_, data) => { ... })
}
