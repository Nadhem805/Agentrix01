import type { IpcMain } from 'electron'

// Handles: post:create, post:update, post:delete, post:list,
//          post:schedule, post:reschedule, post:cancel,
//          media:upload, media:delete, calendar:get
export function registerPostHandlers(_ipcMain: IpcMain) {
  // TODO: implement in Phase 4 + 5
}
